accessWidener v2 official

# MC 26.2 is unobfuscated, so access widener entries use the "official" namespace.
# The previous Yarn-namespace entries are not referenced by the current code and
# have been removed. Add official-named entries here if access widening is needed.